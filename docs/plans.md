# plans

TDDの作業用todoリスト（使い捨て）。

## ライブラリマネージャー

- [ ] お気に入りへ追加機能。

## 重複検出・除去

- [ ] 重複除去に失敗したとき、重複リストウィンドウ内のエラー表示用ラベルにエラーを表示する。
- [x] エラープロパティを追加し、その内容はラベルへバインドされる。
- [x] removeCheckedDuplicatesの例外は、エラープロパティへエラーをセット。
- [x] 作業実行時に、エラープロパティを空にする。
- [x] エラーラベルの文字は赤色。

## その他

- [ ] アプリ起動時のエラー処理。
  - テストリスト
    - [ ] ライブラリDB（SQLite）が開けない場合、エラーダイアログを表示する。
    - [ ] 起動時スキャンが失敗した場合（ライブラリフォルダが読めない等）、エラーダイアログを表示する。現在は無言で握りつぶされる。
