# plans

TDDの作業用todoリスト（使い捨て）。

## 重複候補の一覧表示

- [ ] ID3タグの曲名、アーティストに基づく重複判定。
  - [x] Find by metadataボタンを押すと、メタデータの曲名とアーティスト名がAND一致する場合で絞り込み。
    - [x] 曲名とアーティストが両方同じファイルだけが、重複グループになる。
    - [x] 曲名またはアーティストが空のファイルは、判定から除外される。
    - [x] detectByMetadataは、曲名とアーティストで重複を判定して格納する。
    - [x] Find by Metadataボタンを押すと、メタデータ判定の結果が一覧に格納される。
    - [x] グループの表示名は「曲名 - アーティスト名」。
  - [x] メタデータ判定の削除では、最大サイズの1つを残す（同サイズはリスト先頭を優先）。
    - [x] グループ内のファイルはサイズの大きい順に並び、同サイズは発見順を保つ（先頭が削除時に残る）。

## その他

- [ ] アプリ起動時のエラー処理。
