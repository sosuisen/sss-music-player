# plans

TDDの作業用todoリスト（使い捨て）。

## ウィンドウ

- [ ] Windows 11風のタイトルバー（StageStyle.EXTENDED + HeaderBar、JavaFX 26プレビュー機能）。
  - EXTENDEDの指定自体はヘッドレステストで検証不能（プラットフォームがUNDECORATEDへダウングレードするため）。実装のみ行い、目視確認する。
  - テストリスト
    - [x] ライブラリ管理ウィンドウに、HeaderBarが表示される。

## ライブラリマネージャー

- [ ] お気に入りへ追加機能。

## その他

- [ ] アプリ起動時のエラー処理。
  - テストリスト
    - [ ] ライブラリDB（SQLite）が開けない場合、エラーダイアログを表示する。
    - [ ] 起動時スキャンが失敗した場合（ライブラリフォルダが読めない等）、エラーダイアログを表示する。現在は無言で握りつぶされる。
