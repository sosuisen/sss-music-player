# plans

TDDの作業用todoリスト（使い捨て）。

## ライブラリスキャン

- [x] ID3タグのロード。
  - [x] ロードしたMusicFileにID3タグが含まれる。
  - [x] ライブラリを用いたID3タグのロード。
  - [x] タグの無い・解析できないファイルは空文字タグになる。
  - [x] タグの無い正常なファイルは空文字タグになる。
- [x] メタデータのSQLiteキャッシュ。
  - [x] パス、サイズ、更新日時、メタデータをDBで管理。
    - [x] LibraryDatabaseの実装クラスが、findでマッチする値か空のOptionalを返す。
    - [x] 同じパスを再保存すると上書きされる。
  - [x] スキャン時のキャッシュヒット判定をパス、サイズ、更新日時で行う。
  - [x] スキャン時にキャッシュヒットしたらキャッシュメタデータを利用、キャッシュヒットしなければファイルからロード。
    - [x] DBに保存されるlastModifiedは、作成日時でなく更新日時である。
    - [x] キャッシュヒットしたらキャッシュのメタデータを利用する。
    - [x] キャッシュがヒットしなかった場合、ロードしたメタデータをDBに保存する。
    - [x] SQLite実装をAppに配線するとき、LibraryScannerの無引数コンストラクタを削除する。
      - [x] DBファイルのパスをシステムプロパティsss.library.dbで上書きできる（デフォルトは~/.sss-music-player/library.db）。
      - [x] アプリを起動すると、解決されたパスにライブラリDBファイルが作られる。
      - [x] DBファイルの親フォルダがなければ作成される。
  - [x] スキャン中は、モーダルな「スキャン中」ダイアログを表示。
    - [x] スキャンが始まるとモーダルダイアログを表示して、終わると閉じる。
    - [x] スキャンが失敗してもダイアログが閉じる（scanningがfalseに戻る）。
  - [x] スキャン中ダイアログには、現在リード中のファイルパスをリアルタイム表示。
  - [x] Scanningウィンドウは横幅600、ファイル名は折り返し表示、ウィンドウとの間に20pxの余白。
  - [x] ScanningウィンドウをLibrary Managerよりも前に表示する。
    - [x] Scanningウィンドウのオーナーは、ライブラリ管理ウィンドウである。
    - [x] 起動時はメインウィンドウ表示後にスキャンが開始され、Scanningウィンドウのオーナーはメインウィンドウになる。
    - [x] スキャン時に、読み込むファイルのパスが順にコールバックへ通知される。
    - [x] スキャン中に読み込んだファイルのパスがAppModelのscanningFileに反映される。
    - [x] スキャン中ダイアログに、読み込み中のファイルパスが表示される。
  - [x] テスト用のm4aファイルを作成するヘルパー。
  - [x] m4aファイルのID3情報を取得できる。

## 重複候補の一覧表示

- [ ] ID3タグの曲名、アーティストに基づく重複判定。
  - [x] Find by Metadataボタンを追加。
  - [ ] 詳細パネルでは、各セルの2行目に曲名/アーティスト名を表示。
  - [ ] Find by metadataボタンを押すと、メタデータの曲名とアーティスト名がAND一致する場合で絞り込み。

## その他

- [ ] アプリ起動時のエラー処理。
