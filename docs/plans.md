# plans

TDDの作業用todoリスト（使い捨て）。

## 再生機能

- [ ] 1曲リピート再生。

## ライブラリマネージャー

- [ ] お気に入りへ追加機能。
- [ ] 入力フィールドでアルバムの絞り込み。
- [ ] 曲リストの行に、曲名の後ろに（アーティスト名）を表示。
- [ ] Albumモデルは年情報も持つ。
- [ ] アルバム情報パネルにEditボタンを追加。押すとメタデータ編集ウィンドウが開き、アルバムのメタデータを編集・保存できる。
  - 編集フィールド: アルバム名・アルバムアーティスト・年（アルバムの全トラックに一括適用）。
  - 保存先: 音声ファイルのタグ（保存後に再スキャンで一覧へ反映）。
  - レイアウト: 編集ウィンドウはGridPane（SettingsViewと同様）。
  - テストリスト:
    - [x] アルバム情報パネルにEditボタンが表示され、押すとメタデータ編集ウィンドウが開く。
    - [x] アルバム未選択のとき、Editボタンは無効。
    - [x] タグ書き込み: mp3のタグにアルバム名・アルバムアーティスト・年を書き込める。
    - [x] タグ書き込み: m4aにも同様に書き込める。
    - [x] 編集ウィンドウに、選択中のアルバムのアルバム名・アルバムアーティストが初期表示される（年は自動入力タスクで扱う）。
    - [ ] Saveすると、アルバムの全トラックのタグに編集内容が書き込まれる。
    - [ ] Save後、ライブラリが再スキャンされる。
    - [ ] Save後、編集ウィンドウが閉じる。
    - [ ] Cancelでは何も書き込まれない。
    - [ ] 開いた直後の値と異なる値が入力されているフィールドは、背景が明るい黄色になる。
    - [ ] 開いた後、アルバムアーティストが空の場合、最初の曲のアーティスト名を自動入力する（背景は明るい黄色になる）。
    - [ ] 開いた後、年も同様に自動入力する（背景は明るい黄色になる）。

## その他

- [ ] アプリ起動時のエラー処理。
