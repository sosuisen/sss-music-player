# plans

TDDの作業用todoリスト（使い捨て）。

## 重複候補の一覧表示

- [x] メタデータ重複判定は、曲名・アーティスト名に加えてアルバム名も一致するべき。
  - [x] 曲名とアーティストが同じでも、アルバムが異なるファイルは同じグループにならない。
  - アルバム名が空のファイルは判定に含める（除外は曲名・アーティストが空の場合のみ）。

## その他

- [ ] アプリ起動時のエラー処理。
