package com.sosuisha.presentation.screens.librarymanager;

/**
 * State of the playback controlled by the player panel.
 */
public enum PlayerState {
    /** Nothing is playing. */
    STOPPED,
    /** A track is playing. */
    PLAYING,
    /** Playback is paused. */
    PAUSED
}
